Prompts Used (2–5 max):
1. Instructions
- Write clean Java code with javadocs.
- Focus on maintainable design.
- Follow Template Method pattern correctly.
Template Method Overview
You must design a report system using Template Method with a fixed workflow and variable steps.
Required Workflow
loadData → formatHeader → formatBody → formatFooter → generateReport
Data Requirements
StudentReport: studentName (String), gpa (double)
CourseReport: courseName (String), enrollment (int)
Values must be set in loadData() and used in output.

2. we need to adjest for the driver: Driver Requirements - Use List<Report> - Add StudentReport and CourseReport - Loop through list - Call generateReport() - Demonstrate polymorphism

How AI Helped (2–3 sentences):
Using AI helped me debug build errors due to inconsistent naming of my class and what was used in the Driver. It also made it easy to generate the code with only need for review and little manual typing.

Reflection (1–2 sentences):
I understood the importance of consistent naming.
